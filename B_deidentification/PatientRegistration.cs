using System;
using System.Collections.Generic;
using System.Data.OleDb;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
/*
namespace de_identifying
{
    class PatientRegistration
    {
        public String chartNumber;
        public String beginDate;
        public String endDate;
        public String type;
        public String registerNumber;
        public String code;

        public PatientRegistration(OleDbDataReader reader)
        {
            chartNumber = reader[0].ToString();
            beginDate = reader[1].ToString();
            endDate = reader[2].ToString();
            type = reader[3].ToString();
            registerNumber = reader[4].ToString();
            code = reader[5].ToString();
        }

        public void dump()
        {
            Console.WriteLine(chartNumber + " " + beginDate + " " + endDate + " " + type + " " + registerNumber + " " + code);
        }
    }
}
*/
